# `@vadymkharyton/eslint-config`

> Shared [ESLint](https://eslint.org) config.
## Usage

**Install**:

```bash
$ npm i @vadymkharyton/eslint-config -D
```

**Add `.eslintrc.js` file and fill one with following lines**:

```js
module.exports = {
  extends: ['@vadymkharyton/eslint-config']
};
```